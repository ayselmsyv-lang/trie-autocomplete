# trie-autocomplete

The purpose of this code is finding the word bt entering prefix.

## Input:

We enter the prefix

## Output:

According to our dictionary we search for the words in the dictionary which match with our prefix

For example if we write ```app``` then our code will execute and give outputs such as ```apple, approach, app, apply.```
